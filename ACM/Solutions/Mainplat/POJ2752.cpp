#include<iostream>
#include<cstdio>
#include<cmath>
#include<cstring>
#include<cstdlib>
#include<algorithm>
using namespace std;
typedef long long ll;
const int maxn = 400005;
char mainstr[maxn];
int main()
{
    while(scanf("%s",mainstr) != 0)
    {
        int len = strlen(mainstr);
        cout<<len<<endl;
    }
    return 0;
}

