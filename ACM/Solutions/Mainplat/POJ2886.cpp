#include<iostream>
#include <algorithm>
#include<cstdio>
#include<cstring>
#define maxn 5e5
using namespace std;
typedef long long ll;
int main()
{
    int n, k;
    while(scanf("%d%d", &n, &k) != EOF)
    {
    
    }
    return 0;
}